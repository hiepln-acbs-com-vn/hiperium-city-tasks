import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'hiperium-city-tasks-pwa',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
